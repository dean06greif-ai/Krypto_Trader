aiohttp>=3.9
numpy>=1.26
pandas>=2.0
psutil>=5.9
# Optional - GPU-Beschleunigung (NVIDIA, automatische Erkennung + CPU-Fallback):
#   CUDA 12: pip install cupy-cuda12x
#   CUDA 11: pip install cupy-cuda11x
